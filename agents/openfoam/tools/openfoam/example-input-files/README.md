# Example Input Files

These are example Python scripts that the OpenFOAM agent would generate.
They demonstrate the standard workflow: setup → run → extract metrics.

## Files

- `pipe_flow_example.py` — Internal pipe flow pressure drop calculation
- `cavity_flow_example.py` — Lid-driven cavity benchmark (Re=100)
